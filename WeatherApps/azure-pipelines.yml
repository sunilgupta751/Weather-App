trigger:
- dev

variables:
- group: Variable_Group_Rabbit_Mq_Password   # variable group for secrets like rabbitmqPassword 
- name: imageName
  value: 'weatherapp'
- name: acrName
  value: 'acrlearn001.azurecr.io'
- name: acrserviceconnection
  value: 'acr_serviceconnection'
- name: sshServiceConnection
  value: 'ssh-service-connection'
- name: vmAppPath
  value: '/home/ubuntu/weatherapp'

stages:
- stage: Build
  jobs:
  - job: BuildAndPush
    pool:
      name: 'SelfHosted-AgentPool'
    steps:
    - checkout: self
    - task: Docker@2
      inputs:
        command: buildAndPush
        repository: '$(acrName)/$(imageName)'
        dockerFile: '**/Dockerfile'
        containerRegistry: $(acrserviceconnection)
        tags: |
          $(Build.BuildId)
          latest

- stage: Deploy
  dependsOn: Build
  jobs:
  - job: DeployToVM
    pool:
      name: 'SelfHosted-AgentPool'
    steps:
    - task: SSH@0
      inputs:
        sshEndpoint: $(sshServiceConnection)
        runOptions: commands
        commands: |
          mkdir -p $(vmAppPath)
          cd $(vmAppPath)

          export rabbitmqPassword=$(rabbitmqPassword)

          docker pull $(acrName)/$(imageName):$(Build.BuildId)

          docker-compose down || true
          docker-compose up -d
